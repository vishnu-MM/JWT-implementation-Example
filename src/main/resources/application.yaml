spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 2248
    url: jdbc:postgresql://localhost:5432/jwt_example
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: true
    generate-ddl: true
    open-in-view: false